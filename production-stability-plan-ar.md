# خطة تنفيذ مطلوبة لتحويل التطبيق إلى Production Stable

تاريخ: 2026-04-20
المرجع: طلب العميل على `https://phenomenal-hamster-5b8efe.netlify.app`

## ملاحظة مهمة
لا يوجد كود التطبيق داخل هذا المستودع حاليًا (الموجود فقط ملفات توثيق)، لذلك لا يمكن تنفيذ التعديلات مباشرة على النسخة المنشورة من هذا المكان.

## المطلوب المؤكد (تنفيذ فوري عند توفر السورس)

1) حذف النص `🏪 385 صنف` من الـ Dashboard فقط، بدون تغيير أي منطق حسابي.

2) Offline + Online (إجباري)
- اعتماد `localStorage/IndexedDB` كمصدر أساسي.
- Firebase يكون للمزامنة فقط عند توفر الإنترنت.
- إضافة Service Worker + Cache للملفات الأساسية.
- Queue للعمليات أثناء Offline ثم Sync تلقائي عند عودة الاتصال.

3) Auto Backup (حماية 100%)
- حفظ تلقائي كل 5 دقائق.
- حفظ Snapshot قبل أي Import.
- استرجاع تلقائي من آخر نسخة سليمة عند فشل/Crash.

4) Crash Protection
- لف العمليات الحساسة بـ Guard موحد (تسجيل الخطأ + Rollback آمن + رسالة للمستخدم).
- حماية parsing/serialization للبيانات قبل الحفظ.

5) البحث الشامل
- البحث بالاسم.
- البحث برقم العميل.
- البحث برقم الفاتورة.
- التطبيق على: العملاء، المبيعات، الفواتير، والأرشيف.

6) تنبيه المديونية بعد 15 يوم (Online/Offline)
- Job محلي يومي يفحص تواريخ الاستحقاق.
- Flag `needsCollection=true` للعملاء المتأخرين.
- إشعار واضح داخل لوحة المتابعة سواء متصل أو غير متصل.

7) أرشفة العميل المسدد مع إمكانية الاسترجاع
- نقل العميل المسدد لقسم أرشيف مع كل التفاصيل.
- زر استرجاع يعيد نفس السجل بدون فقد بيانات.
- بحث داخل الأرشيف (اسم/رقم عميل).

8) ربح حسب القسم (بدل ربح عام فقط)
- أقسام مستقلة: توك توك / بطاريات / مفاتيح / قطع غيار.
- إمكانية تعديل الربح لكل قسم على حدة.
- خيار اختياري لتطبيق نسبة على كل الأقسام (Bulk Apply).

9) شريط علوي ثابت للمؤشرات المطلوبة
- `دين بطاريات` (إجمالي وليس يومي).
- `دين توك توك` (إجمالي وليس يومي).
- تحديث لحظي عبر `updateDashboard()` بعد كل عملية.

10) شريط ثابت للأوامر السريعة
- `إضافة صنف`
- `واتساب`
- يظل ثابتًا أثناء التمرير.

## قواعد الثبات (غير قابلة للكسر)
- عدم حذف أي كود قائم (تعديل غير هدّام فقط).
- عدم تغيير سلوك الوظائف الحالية.
- نفس القيم الحسابية الحالية (No Logic Drift).
- كل تعديل يمر عبر اختبارات: وحدة + تكامل + سيناريو Offline.

## خطة اختبار القبول (Acceptance)
1. إنشاء/تعديل/بيع أثناء Offline ثم إعادة الاتصال والتأكد من Sync بدون تعارض.
2. اختبار استرجاع بعد Crash باستخدام آخر Backup تلقائي.
3. Import فاسد -> يجب إنشاء Backup قبل التنفيذ وعدم فقد بيانات.
4. عميل عليه دين >15 يوم يظهر في المطالبة Online/Offline.
5. عميل مسدد -> أرشفة -> استرجاع -> ظهور كامل البيانات.
6. البحث (اسم/رقم عميل/رقم فاتورة) يعمل في كل الأقسام المذكورة.
7. عدم ظهور `🏪 385 صنف` نهائيًا من الـ Dashboard.

## المطلوب لتطبيق الإصلاح فعليًا
- رفع سورس المشروع (أو ربط المستودع الصحيح الذي يحتوي ملفات الواجهة والمنطق).
- بعدها يتم تنفيذ البنود السابقة Patch-by-Patch بدون كسر الوظائف.
